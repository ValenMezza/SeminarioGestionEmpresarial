<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Usuarios</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partials/sidebar.ejs') %>
    <main id="config-page">

        <div class="page-header">
            <div>
                <a href="/configuraciones" class="back-link">← Configuraciones</a>
                <h1 class="page-title">Usuarios del sistema</h1>
            </div>
            <button type="button" class="btn-primary-green" id="btnNuevoUsuario">+ Nuevo usuario</button>
        </div>

        <!-- Toggle filtro -->
        <div class="filtros-bar" style="margin-bottom:1rem">
            <label class="toggle-row">
                <input type="checkbox" id="toggleMostrarPausados" checked>
                <span>Mostrar usuarios pausados</span>
            </label>
        </div>

        <!-- Tabla de usuarios -->
        <div class="table-wrapper">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>#ID</th>
                        <th>Nombre</th>
                        <th>Apellido</th>
                        <th>Usuario</th>
                        <th>Rol</th>
                        <th>Estado</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody id="tablaUsuarios">
                    <% if (usuarios.length === 0) { %>
                        <tr>
                            <td colspan="7" class="td-vacia">No hay usuarios registrados</td>
                        </tr>
                    <% } %>
                    <% usuarios.forEach(u => { %>
                        <tr data-activo="<%= u.activo %>">
                            <td>#<%= u.id %></td>
                            <td><%= u.nombre || '—' %></td>
                            <td><%= u.apellido || '—' %></td>
                            <td><b><%= u.user %></b></td>
                            <td>
                                <span class="badge badge--rol-<%= u.rol %>">
                                    <%= u.rol === 'admin' ? 'Administrador' : 'Operador' %>
                                </span>
                            </td>
                            <td>
                                <span class="badge badge--<%= u.activo ? 'con-stock' : 'sin-stock' %>">
                                    <%= u.activo ? 'Activo' : 'Pausado' %>
                                </span>
                            </td>
                            <td class="acciones-col">
                                <form action="/configuraciones/usuarios/pausar/<%= u.id %>" method="POST" style="display:inline">
                                    <button type="submit" class="btn-secondary btn-sm">
                                        <%= u.activo ? 'Pausar' : 'Activar' %>
                                    </button>
                                </form>
                                <button type="button" class="btn-secondary-yellow btn-sm btn-cambiar-pass"
                                    data-id="<%= u.id %>"
                                    data-user="<%= u.user %>">
                                    Cambiar pass
                                </button>
                                <% if (u.id !== 1) { %>
                                <form action="/configuraciones/usuarios/eliminar/<%= u.id %>" method="POST" style="display:inline"
                                      onsubmit="return confirm('¿Eliminar usuario <%= u.user %>?')">
                                    <button type="submit" class="btn-danger btn-sm">Eliminar</button>
                                </form>
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

    </main>

    <!-- Modal: Crear nuevo usuario -->
    <div id="modal-nuevo-usuario" class="modal-overlay" style="display:none">
        <div class="modal-box">
            <div class="modal-header">
                <h2 class="modal-title">Nuevo usuario</h2>
                <button type="button" class="modal-close" id="cerrarModalUsuario">✕</button>
            </div>

            <form action="/configuraciones/usuarios/crear" method="POST" class="form-grid" id="formNuevoUsuario">
                <div class="form-row-2">
                    <div class="form-group">
                        <label for="nombre">Nombre *</label>
                        <input type="text" name="nombre" id="nombre" placeholder="Ej: Juan" required>
                    </div>
                    <div class="form-group">
                        <label for="apellido">Apellido *</label>
                        <input type="text" name="apellido" id="apellido" placeholder="Ej: Perez" required>
                    </div>
                </div>

                <div class="form-group">
                    <label for="user">Nombre de usuario *</label>
                    <input type="text" name="user" id="user" placeholder="Ej: operador1" required>
                </div>

                <div class="form-group">
                    <label for="password">Contraseña *</label>
                    <input type="password" name="password" id="password" placeholder="Min. 6 caracteres" required>
                </div>

                <div class="form-group">
                    <label for="rol">Rol</label>
                    <select name="rol" id="rol">
                        <option value="operador">Operador</option>
                        <option value="admin">Administrador</option>
                    </select>
                </div>

                <div class="modal-actions">
                    <button type="submit" class="btn-primary">Crear usuario</button>
                    <button type="button" class="btn-secondary" id="cancelarModalUsuario">Cancelar</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Modal: Cambiar contraseña -->
    <div id="modal-cambiar-pass" class="modal-overlay" style="display:none">
        <div class="modal-box">
            <div class="modal-header">
                <h2 class="modal-title">Cambiar contraseña — <span id="modal-pass-user"></span></h2>
                <button type="button" class="modal-close" id="cerrarModalPass">✕</button>
            </div>
            <form id="formCambiarPass" method="POST" style="margin-top:1rem">
                <div class="form-group">
                    <label for="inputNuevoPass">Nueva contraseña *</label>
                    <input type="password" name="password" id="inputNuevoPass" placeholder="Min. 4 caracteres" required minlength="4" autocomplete="new-password">
                </div>
                <div class="form-group" style="margin-top:0.5rem">
                    <label for="inputConfirmarPass">Confirmar contraseña *</label>
                    <input type="password" id="inputConfirmarPass" placeholder="Repetir contraseña" required>
                </div>
                <p id="modal-pass-error" style="color:var(--danger);font-size:0.85rem;margin:0.5rem 0 0;display:none">Las contraseñas no coinciden.</p>
                <div class="modal-actions" style="margin-top:1.25rem">
                    <button type="submit" class="btn-primary">Guardar contraseña</button>
                    <button type="button" class="btn-secondary" id="cancelarModalPass">Cancelar</button>
                </div>
            </form>
        </div>
    </div>

    <script src="/javascript/formValidation.js"></script>
    <script src="/javascript/sidebar.js"></script>
    <script>
        // Modal nuevo usuario
        const modalUsuario = document.getElementById('modal-nuevo-usuario');
        document.getElementById('btnNuevoUsuario').addEventListener('click', () => {
            modalUsuario.style.display = 'flex';
            document.body.style.overflow = 'hidden';
        });
        function cerrarModal() {
            modalUsuario.style.display = 'none';
            document.body.style.overflow = '';
        }
        document.getElementById('cerrarModalUsuario').addEventListener('click', cerrarModal);
        document.getElementById('cancelarModalUsuario').addEventListener('click', cerrarModal);

        // Validacion
        document.getElementById('formNuevoUsuario').addEventListener('submit', function(e) {
            if (!validarFormulario(this, ['#nombre', '#apellido', '#user', '#password'])) {
                e.preventDefault();
            }
        });

        // Modal cambiar contraseña
        const modalPass     = document.getElementById('modal-cambiar-pass');
        const formCambiarPass = document.getElementById('formCambiarPass');
        const inputNuevoPass  = document.getElementById('inputNuevoPass');
        const inputConfirmar  = document.getElementById('inputConfirmarPass');
        const errorPass       = document.getElementById('modal-pass-error');

        document.querySelectorAll('.btn-cambiar-pass').forEach(btn => {
            btn.addEventListener('click', () => {
                formCambiarPass.action = `/configuraciones/usuarios/cambiar-pass/${btn.dataset.id}`;
                document.getElementById('modal-pass-user').textContent = btn.dataset.user;
                inputNuevoPass.value = '';
                inputConfirmar.value = '';
                errorPass.style.display = 'none';
                modalPass.style.display = 'flex';
                document.body.style.overflow = 'hidden';
                inputNuevoPass.focus();
            });
        });

        function cerrarModalPass() {
            modalPass.style.display = 'none';
            document.body.style.overflow = '';
        }
        document.getElementById('cerrarModalPass').addEventListener('click', cerrarModalPass);
        document.getElementById('cancelarModalPass').addEventListener('click', cerrarModalPass);
        modalPass.addEventListener('click', e => { if (e.target === e.currentTarget) cerrarModalPass(); });

        formCambiarPass.addEventListener('submit', function(e) {
            errorPass.style.display = 'none';
            if (inputNuevoPass.value !== inputConfirmar.value) {
                e.preventDefault();
                errorPass.style.display = 'block';
                inputConfirmar.focus();
            }
        });

        // Toggle pausados
        const toggle = document.getElementById('toggleMostrarPausados');
        toggle.addEventListener('change', () => {
            const filas = document.querySelectorAll('#tablaUsuarios tr[data-activo]');
            filas.forEach(fila => {
                if (fila.dataset.activo === 'false') {
                    fila.style.display = toggle.checked ? '' : 'none';
                }
            });
        });
    </script>
</body>
</html>
